---
layout: layout.njk
title:
featureTitle: 2024 Releases
featureText: Lorem ipsum dolor sit amet, consectetur adipisicing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
quoteText: A writer only begins a book. A reader finishes it.
quoteSource: Samuel Johnson
---

{% set booksArr = [] %}
{% for k, book in books %}
  {% if book.isFeatured %}
    {% set booksArr = (booksArr.push(book), booksArr) %}
  {% endif %}
{% endfor %}

<div id="home-feature" class="flush-with-header">
  <div class="container">
    <div class="row justify-content-end">
      <div class="col-8 col-sm-8 col-md-9 col-lg-4 col-xl-5">
        <h2>{{ featureTitle }}</h2>
        <p class="d-none d-sm-block">{{ featureText }}</p>
      </div>
      <div class="col-12 col-lg-4 col-xl-5">
        <div id="home-feature-books">
          {% for book in booksArr | sort(false, false, "publishDate") | reverse %}
            <a href="{{ '/books/' | url }}{{ book.title | slugify | url }}">
              <img
              src="{{ book.coverImageUrl | url }}"
              class="img-fluid shadow"
              alt="{{ book.title }}"/>
            </a>
          {% endfor %}
        </div>
      </div>
    </div>
  </div>
</div>
<div class="container">
  <p class="d-sm-none" aria-hidden="true">{{ featureText }}</p>
  {% include "quote.njk" %}
  <small class="text-body-tertiary">Photo Artistry by Melinda Lamm</small>
</div>
